export const DISCIPLINE_COLOR_PALETTE = [
  {
    id: 'red',
    hex: '#dc2626',
    bg: 'bg-red-600',
    text: 'text-red-700',
    border: 'border-red-100',
    light: 'bg-red-50/50',
    soft: 'bg-red-50 text-red-700 border-red-200 dark:bg-red-950/25 dark:text-red-300 dark:border-red-900/40',
    progress: 'bg-gradient-to-r from-red-600 to-red-800',
  },
  {
    id: 'blue',
    hex: '#2563eb',
    bg: 'bg-blue-600',
    text: 'text-blue-700',
    border: 'border-blue-100',
    light: 'bg-blue-50/50',
    soft: 'bg-blue-50 text-blue-700 border-blue-200 dark:bg-blue-950/25 dark:text-blue-300 dark:border-blue-900/40',
    progress: 'bg-gradient-to-r from-blue-600 to-blue-800',
  },
  {
    id: 'green',
    hex: '#16a34a',
    bg: 'bg-green-600',
    text: 'text-green-700',
    border: 'border-green-100',
    light: 'bg-green-50/50',
    soft: 'bg-green-50 text-green-700 border-green-200 dark:bg-green-950/25 dark:text-green-300 dark:border-green-900/40',
    progress: 'bg-gradient-to-r from-green-600 to-green-800',
  },
  {
    id: 'yellow',
    hex: '#ca8a04',
    bg: 'bg-yellow-600',
    text: 'text-yellow-700',
    border: 'border-yellow-200',
    light: 'bg-yellow-50/50',
    soft: 'bg-yellow-50 text-yellow-800 border-yellow-200 dark:bg-yellow-950/25 dark:text-yellow-300 dark:border-yellow-900/40',
    progress: 'bg-gradient-to-r from-yellow-500 to-yellow-700',
  },
  {
    id: 'purple',
    hex: '#7e22ce',
    bg: 'bg-purple-700',
    text: 'text-purple-700',
    border: 'border-purple-100',
    light: 'bg-purple-50/50',
    soft: 'bg-purple-50 text-purple-700 border-purple-200 dark:bg-purple-950/25 dark:text-purple-300 dark:border-purple-900/40',
    progress: 'bg-gradient-to-r from-purple-600 to-purple-800',
  },
  {
    id: 'magenta',
    hex: '#db2777',
    bg: 'bg-pink-600',
    text: 'text-pink-700',
    border: 'border-pink-100',
    light: 'bg-pink-50/50',
    soft: 'bg-pink-50 text-pink-700 border-pink-200 dark:bg-pink-950/25 dark:text-pink-300 dark:border-pink-900/40',
    progress: 'bg-gradient-to-r from-pink-600 to-pink-800',
  },
  {
    id: 'cyan',
    hex: '#0891b2',
    bg: 'bg-cyan-600',
    text: 'text-cyan-700',
    border: 'border-cyan-100',
    light: 'bg-cyan-50/50',
    soft: 'bg-cyan-50 text-cyan-700 border-cyan-200 dark:bg-cyan-950/25 dark:text-cyan-300 dark:border-cyan-900/40',
    progress: 'bg-gradient-to-r from-cyan-600 to-cyan-800',
  },
  {
    id: 'orange',
    hex: '#ea580c',
    bg: 'bg-orange-600',
    text: 'text-orange-700',
    border: 'border-orange-100',
    light: 'bg-orange-50/50',
    soft: 'bg-orange-50 text-orange-700 border-orange-200 dark:bg-orange-950/25 dark:text-orange-300 dark:border-orange-900/40',
    progress: 'bg-gradient-to-r from-orange-600 to-orange-800',
  },
  {
    id: 'slate',
    hex: '#475569',
    bg: 'bg-slate-600',
    text: 'text-slate-700',
    border: 'border-slate-200',
    light: 'bg-slate-50/50',
    soft: 'bg-slate-50 text-slate-700 border-slate-200 dark:bg-slate-950/25 dark:text-slate-300 dark:border-slate-800/40',
    progress: 'bg-gradient-to-r from-slate-600 to-slate-800',
  },
  {
    id: 'fuchsia',
    hex: '#c026d3',
    bg: 'bg-fuchsia-600',
    text: 'text-fuchsia-700',
    border: 'border-fuchsia-100',
    light: 'bg-fuchsia-50/50',
    soft: 'bg-fuchsia-50 text-fuchsia-700 border-fuchsia-200 dark:bg-fuchsia-950/25 dark:text-fuchsia-300 dark:border-fuchsia-900/40',
    progress: 'bg-gradient-to-r from-fuchsia-600 to-fuchsia-800',
  },
  {
    id: 'lime',
    hex: '#65a30d',
    bg: 'bg-lime-600',
    text: 'text-lime-700',
    border: 'border-lime-100',
    light: 'bg-lime-50/50',
    soft: 'bg-lime-50 text-lime-700 border-lime-200 dark:bg-lime-950/25 dark:text-lime-300 dark:border-lime-900/40',
    progress: 'bg-gradient-to-r from-lime-600 to-lime-800',
  },
  {
    id: 'violet',
    hex: '#6d28d9',
    bg: 'bg-violet-700',
    text: 'text-violet-700',
    border: 'border-violet-100',
    light: 'bg-violet-50/50',
    soft: 'bg-violet-50 text-violet-700 border-violet-200 dark:bg-violet-950/25 dark:text-violet-300 dark:border-violet-900/40',
    progress: 'bg-gradient-to-r from-violet-600 to-violet-800',
  },
  {
    id: 'teal',
    hex: '#0f766e',
    bg: 'bg-teal-700',
    text: 'text-teal-700',
    border: 'border-teal-100',
    light: 'bg-teal-50/50',
    soft: 'bg-teal-50 text-teal-700 border-teal-200 dark:bg-teal-950/25 dark:text-teal-300 dark:border-teal-900/40',
    progress: 'bg-gradient-to-r from-teal-600 to-teal-800',
  },
  {
    id: 'rose',
    hex: '#e11d48',
    bg: 'bg-rose-600',
    text: 'text-rose-700',
    border: 'border-rose-100',
    light: 'bg-rose-50/50',
    soft: 'bg-rose-50 text-rose-700 border-rose-200 dark:bg-rose-950/25 dark:text-rose-300 dark:border-rose-900/40',
    progress: 'bg-gradient-to-r from-rose-600 to-rose-800',
  },
  {
    id: 'stone',
    hex: '#57534e',
    bg: 'bg-stone-600',
    text: 'text-stone-700',
    border: 'border-stone-200',
    light: 'bg-stone-50/50',
    soft: 'bg-stone-50 text-stone-700 border-stone-200 dark:bg-stone-950/25 dark:text-stone-300 dark:border-stone-800/40',
    progress: 'bg-gradient-to-r from-stone-600 to-stone-800',
  },
];

export const REVIEW_COLOR = {
  id: 'review',
  hex: '#3b82f6',
  bg: 'bg-blue-500',
  text: 'text-blue-600',
  border: 'border-blue-100',
  light: 'bg-blue-50/50',
  soft: 'bg-blue-50 text-blue-700 border-blue-200 dark:bg-blue-950/25 dark:text-blue-300 dark:border-blue-900/40',
  progress: 'bg-gradient-to-r from-blue-500 to-blue-700',
};

const disciplineColorAssignments = new Map();

export const getDisciplineKey = (disciplina) => {
  const value = typeof disciplina === 'object'
    ? (disciplina?.id || disciplina?.nome || disciplina?.disciplinaNome || disciplina?.disciplina)
    : disciplina;

  return String(value || 'disciplina')
    .trim()
    .toLocaleLowerCase('pt-BR');
};

const hexToRgb = (hex = '#3b82f6') => {
  const normalized = String(hex).replace('#', '');
  const parsed = parseInt(normalized.length === 3
    ? normalized.split('').map((char) => `${char}${char}`).join('')
    : normalized, 16);

  if (Number.isNaN(parsed)) return '59, 130, 246';

  return [
    (parsed >> 16) & 255,
    (parsed >> 8) & 255,
    parsed & 255,
  ].join(', ');
};

export const getDisciplineColor = (disciplina) => {
  const key = getDisciplineKey(disciplina);

  if (!disciplineColorAssignments.has(key)) {
    const color = DISCIPLINE_COLOR_PALETTE[
      disciplineColorAssignments.size % DISCIPLINE_COLOR_PALETTE.length
    ];
    disciplineColorAssignments.set(key, color);
  }

  return disciplineColorAssignments.get(key);
};

export const getColorByHex = (hex) => {
  if (!hex) return null;
  const normalized = String(hex).trim().toLocaleLowerCase('pt-BR');
  return DISCIPLINE_COLOR_PALETTE.find((color) => color.hex.toLocaleLowerCase('pt-BR') === normalized)
    || (REVIEW_COLOR.hex.toLocaleLowerCase('pt-BR') === normalized ? REVIEW_COLOR : null);
};

export const getColorById = (id) => {
  if (!id) return null;
  const normalized = String(id).trim().toLocaleLowerCase('pt-BR');
  return DISCIPLINE_COLOR_PALETTE.find((color) => color.id === normalized)
    || (REVIEW_COLOR.id === normalized ? REVIEW_COLOR : null);
};

export const getStoredDisciplineColor = (cor) => {
  if (!cor) return null;
  if (typeof cor === 'object') {
    return getColorById(cor.id) || getColorByHex(cor.hex) || cor;
  }
  return getColorByHex(cor) || getColorById(cor);
};

export const buildDisciplineColorMap = (disciplinas = []) => {
  const map = {};
  let colorIndex = 0;

  disciplinas.forEach((disciplina) => {
    const keys = [
      disciplina?.id,
      disciplina?.nome,
      disciplina?.disciplinaNome,
      disciplina?.disciplina,
    ].filter(Boolean).map(getDisciplineKey);

    if (keys.length === 0 || keys.some((key) => map[key])) return;

    const color = getStoredDisciplineColor(disciplina?.cor)
      || DISCIPLINE_COLOR_PALETTE[colorIndex % DISCIPLINE_COLOR_PALETTE.length];
    colorIndex += 1;
    keys.forEach((key) => { map[key] = color; });
  });

  return map;
};

export const getDisciplineColorForSlot = (slot, colorMap = null) => {
  if (slot?.isRevisao || slot?.isRevisaoAuto || slot?.isConsolidada) return REVIEW_COLOR;

  const keys = [
    slot?.disciplinaId,
    slot?.disciplinaNome,
    slot?.disciplina,
    slot?.disc?.id,
    slot?.disc?.nome,
  ].filter(Boolean).map(getDisciplineKey);

  const mappedColor = colorMap && keys.map((key) => colorMap[key]).find(Boolean);
  return mappedColor
    || getStoredDisciplineColor(slot?.cor)
    || getDisciplineColor(slot?.disciplinaId || slot?.disciplinaNome || slot?.disciplina || slot?.disc);
};

export const getDisciplineCardVars = (color) => ({
  '--discipline-rgb': hexToRgb(color?.hex),
});
