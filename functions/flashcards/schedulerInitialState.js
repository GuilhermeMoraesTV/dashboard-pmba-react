/**
 * Adaptador server-side do contrato congelado CardScheduler.createInitialState().
 * Nao implementa calculo de agendamento; apenas materializa o estado opaco inicial vigente.
 */
module.exports = require('./scheduler');
